# 实现重点特性

## 目的
该示例聚焦 `shared_ptr` 与 `weak_ptr` 的所有权与生命周期管理模型。

## 关键实现点
- 通过作用域变化展示 `shared_ptr` 引用计数的增减过程。
- 在双向关系中使用 `weak_ptr` 断开循环引用链。
- 使用 `lock()` 安全获取临时强引用后再访问对象。
- 使用 `expired()` 判断被观察对象是否已经释放。
- 通过构造/析构日志观察对象真实销毁时机。

## 实践价值
- 说明如何在对象图关系中避免内存泄漏并保持访问安全。
