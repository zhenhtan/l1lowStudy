# 实现重点特性

## 目的
该示例用最小代码演示 RAII 如何在正常路径与异常路径下自动释放资源。

## 关键实现点
- `FileGuard` 构造时获取 `FILE*`，析构时自动关闭。
- 打开失败时构造函数直接抛异常，避免半初始化状态。
- 删除拷贝构造和拷贝赋值，保持资源唯一所有权语义。
- 通过 `WriteLine` 封装写操作，减少裸句柄外泄。
- 同时演示正常执行路径与抛异常路径下的自动清理。
- 主函数中分层异常处理，展示“就近捕获 + 无泄漏退出”。

## 实践价值
- 强化异常安全编程范式，减少手动 `close/free` 漏调用风险。
