# 实现重点特性

## 目的
该示例通过基准对比演示 false sharing 对并发性能的影响。

## 关键实现点
- 提供两种计数器布局：普通连续布局与 `alignas(64)` 缓存行对齐布局。
- 每个线程只更新自己的计数器槽位，隔离算法逻辑变量。
- 采用 `memory_order_relaxed`，把关注点放在缓存一致性流量上。
- 统计两种布局耗时并输出加速比。
- 线程数基于硬件并发度自动适配，便于在不同机器观察差异。

## 实践价值
- 直观体现“数据布局”本身就是并发性能设计的重要部分。
