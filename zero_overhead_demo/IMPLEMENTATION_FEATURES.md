# 实现重点特性

## 目的
该示例从调用路径角度对比“运行时多态”与“编译期多态”的零开销抽象思想。

## 关键实现点
- 运行时路径：接口类 `IOp` + `virtual Apply`，通过 vtable 分发。
- 编译期路径：模板 `SumStatic` + 具体仿函数，调用可在编译期绑定并内联。
- 两条路径处理同一输入并输出一致结果，便于公平对照。
- 代码结构把“算法逻辑”与“分发机制”解耦，突出成本模型差异。

## 实践价值
- 帮助理解抽象方式选择如何影响间接调用开销与编译器优化空间。
