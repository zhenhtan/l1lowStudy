# 实现重点特性

## 目的
该示例对比 CRTP（静态多态）与虚函数（动态多态）的实现差异与代价模型。

## 关键实现点
- 通过 `AnimalCRTP<Derived>` 在编译期绑定派生行为。
- 派生类提供 `speak_impl/move_impl`，由 `static_cast` 委派调用。
- 泛型函数模板直接处理具体类型，便于编译器内联优化。
- 同时保留虚函数版本用于运行时分发对照。
- 清晰呈现“性能与灵活性”的取舍：零额外分发开销 vs 异构容器能力。

## 实践价值
- 帮助理解在性能敏感场景下如何选择多态实现方式。
