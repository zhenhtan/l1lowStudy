# 实现重点特性

## 目的
该示例通过自管理缓冲区，直观对比值语义（拷贝）与移动语义（转移所有权）。

## 关键实现点
- 实现深拷贝构造与拷贝赋值，保证值语义正确性。
- 实现 `noexcept` 移动构造与移动赋值，降低不必要复制开销。
- 被移动对象重置为空状态，维持类不变式。
- 通过日志输出明确观察拷贝/移动触发时机。
- 工厂返回值路径可观察 RVO 或 move 行为。
- 通过按值传参函数对比 copy 与 move 入参效果。

## 实践价值
- 有助于理解 Rule of Five、资源管理与性能优化之间的关系。
